name: Aiven Wake Up

on:
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch:

jobs:
  wakeup:
    runs-on: ubuntu-latest

    steps:
      - name: Fetch Programs
        run: |
          curl --fail --silent --show-error \
          "https://lms-8r47.onrender.com/api/auth/getPrograms"
